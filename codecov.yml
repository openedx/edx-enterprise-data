coverage:
  status:
    project:
      default:
        target: auto
        # One-time coverage-measurement shift from this migration; see PR description.
    patch:
      default:
        # Already passing under the default policy; see PR description.
        target: auto

comment: false
