[tox]
envlist = {data, reporting}-django{42}
requires =
    tox-uv>=1

[wheel]
universal = 1

[doc8]
max-line-length = 120

[pep8]
ignore = E266

[pycodestyle]
exclude = .git,.tox,migrations,settings
max-line-length = 120

[pydocstyle]
ignore = D101,D106,D200,D203,D212,D213,D406,D407,D411,D412,D413
match-dir = (?!migrations)

[testenv]
runner = uv-venv-lock-runner
dependency_groups =
    data: test
    reporting: test-reporting
setenv =
    DJANGO_SETTINGS_MODULE=enterprise_data.settings.test
deps =
    django42: Django>=4.2,<4.3
commands =
    data: pytest -Wd {posargs} --ignore src/enterprise_reporting/
    reporting: pytest -Wd --cov enterprise_reporting --cov-report term-missing --cov-report xml src/enterprise_reporting/tests

[testenv:quality]
runner = uv-venv-lock-runner
dependency_groups =
    quality
setenv =
    DJANGO_SETTINGS_MODULE=enterprise_data.settings.test
allowlist_externals =
    make
    rm
    touch
commands =
    isort --check-only src/enterprise_data src/enterprise_data_roles manage.py
    touch tests/__init__.py
    pylint -j 0 src/enterprise_data src/enterprise_data_roles
    rm tests/__init__.py
    pycodestyle src/enterprise_data src/enterprise_data_roles
    pydocstyle src/enterprise_data src/enterprise_data_roles

